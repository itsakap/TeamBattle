<!-- _form.html.erb -->
<%= form_for [:team_captain, @game] do |f| %>
  <p class="form-group">
    <%= f.label :name, "Game Name" %>
    <%= f.text_field :name, class: "form-control" %>
  </p>

  <p>
    <%= f.submit class: "btn-btn-primary" %>
  </p>
<% end %>